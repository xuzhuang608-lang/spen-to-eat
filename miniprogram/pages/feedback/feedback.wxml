<view class="page">
  <view class="section-title">给饭点转转一点反馈</view>
  <view class="chip-row">
    <view class="chip {{type === item ? 'active' : ''}}" wx:for="{{types}}" wx:key="*this" data-type="{{item}}" bindtap="onSelectType">{{item}}</view>
  </view>

  <textarea class="textarea" value="{{content}}" placeholder="可以简单说说哪里不准，或者你想加什么美食。" bindinput="onInput" />
  <button class="primary-button" bindtap="onSubmit">提交反馈</button>
</view>

