<view class="page">
  <view class="tabs">
    <view class="tab {{tab === 'favorite' ? 'active' : ''}}" data-tab="favorite" bindtap="onSwitchTab">收藏</view>
    <view class="tab {{tab === 'history' ? 'active' : ''}}" data-tab="history" bindtap="onSwitchTab">历史</view>
  </view>

  <view wx:if="{{!dishes.length}}" class="empty">还没有内容，先去转一次吧。</view>
  <view class="dish-list">
    <view class="panel dish-item" wx:for="{{dishes}}" wx:key="id" data-id="{{item.id}}" bindtap="onOpenDish">
      <view>
        <view class="dish-name">{{item.name}}</view>
        <view class="muted">{{item.city}} · {{item.category}} · {{item.taste}}</view>
      </view>
      <view class="arrow">›</view>
    </view>
  </view>

  <button class="primary-button poll-button" bindtap="onCreatePoll">用这些组饭局</button>

  <dish-sheet visible="{{dishSheetVisible}}" dish="{{detailDish}}" bind:close="onCloseDishSheet" />
</view>
